/** @format */

// 10.12 Math.PI()

// Suppose if we find the circumference of a circle using pi
// Formula for circumference of a cirlce is 2PIr

const calculateCircumference = (radius) => {
  return 2 * Math.PI * radius;
};
console.log(calculateCircumference(3));
