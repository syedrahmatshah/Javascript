/** @format */

// / 11 The JavaScript string slice() method is used to fetch the part of the string and returns the new string. It required to specify the index number as the start
// and end parameters to fetch the part of the string. The index starts from 0.

// 11.1 we will print the part of the string by passing starting and ending index.
// let str = "javatpoint";
// console.log(str.slice(2, 5));

// 11.2 we will provide starting index only. In such case, the method fetches the string up to its length.
// let str = "javatpoint";
// console.log(str.slice(0));

// 11.3 we will provide starting index only.
// let str = "javatpoint";
// console.log(str.slice(4));

// 11.4  we will provide negative number as an index. In such case,
// the method starts fetching from the end of the string.
// let str = "javatpoint";
// console.log(str.slice(-5));

//11.5 In this example, we will provide negative number as a starting and ending index.
// In such case, the method starts fetching from the end of the string.
// let str = "javatpoint";
// console.log(str.slice(-5, -1));
